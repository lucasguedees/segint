rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Permite leitura e gravação completa no Firestore da nuvem para sincronização em múltiplos computadores
    match /{document=**} {
      allow read, write: if true;
    }
  }
}

